*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
# NOTE: /vendor is deliberately COMMITTED. Hostinger git deploy is pull-only
# (no composer on the server), so the repo must carry its own dependencies.
Homestead.json
Homestead.yaml
Thumbs.db
